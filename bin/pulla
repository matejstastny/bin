#!/bin/bash

# pulla = pull all (branches)
# script to create local branches for all remote branches in a repo

git branch -r | grep -v '\->' | sed "s,\x1B\[[0-9;]*[a-zA-Z],,g" | while read remote; do git branch --track "${remote#origin/}" "$remote"; done
